// Legacy stub — ProfileSetup replaced by LoginScreen.
// Kept to avoid import errors from any remaining references.
export default function ProfileSetup(_props: { open: boolean }) {
  return null;
}
